---
description: Set up a context/ skeleton and a lean root AGENTS.md that works as a table of contents
---

Use the **context-scaffold** skill to prepare this repository for agent work.

Start with reconnaissance: what already exists (AGENTS.md, CLAUDE.md, context/, .cursor/,
.github/). Do not overwrite anything without asking.

The root should be a **table of contents, not an encyclopedia** - commands copied from the
real package.json/Makefile, not invented. Do not make up a PRD or roadmap: if they are not
in the repo, leave stubs and tell me they will emerge from real work.
